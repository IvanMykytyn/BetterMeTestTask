# 🚀 Frontend

Frontend частина проєкту побудована на:

- React 19
- Vite 7
- Mantine 8
- TanStack React Query
- React Hook Form + Zod
- TailwindCSS 4
- pnpm

---

# 📦 Вимоги

Перед запуском потрібно встановити:

- Node.js 18 або новіше
- pnpm 10 або новіше

Перевірити версії:

```bash
node -v
pnpm -v

Якщо pnpm не встановлений:

npm install -g pnpm
🧑‍💻 Локальний запуск (Development)
1. Клонувати репозиторій
git clone <repo-url>
cd frontend
2. Встановити залежності
pnpm install
3. Запустити dev сервер
pnpm dev

Після запуску додаток буде доступний за адресою:

http://localhost:5173
🏗 Production білд
1. Зібрати проєкт
pnpm build

Ця команда:

компілює TypeScript

створює production-збірку через Vite

Зібрані файли знаходяться у папці:

dist/
2. Перевірити production-збірку локально
pnpm preview

Додаток буде доступний за адресою:

http://localhost:4173
🧹 Лінтинг і форматування
Перевірити код через ESLint
pnpm lint
Відформатувати код через Prettier
pnpm format
Перевірити форматування без змін
pnpm format:check
🌍 Змінні середовища

Якщо використовується API або інші змінні, створіть файл:

.env

Приклад:

VITE_API_URL=http://localhost:3000

У коді доступ через:

import.meta.env.VITE_API_URL
📁 Структура проєкту
src/
 ├── components/
 ├── hooks/
 ├── pages/
 ├── lib/
 ├── types/
 └── main.tsx
